/**
 * Final exam problem bank — OS edition.
 *
 * Phase 2.2: cleared. Phase 2.3 will populate hand-authored MCQ, short-answer,
 * scenario, trace-walk, and pseudocode prompts. The KB compiler does NOT
 * overwrite this file; it is merged into the generated quiz set by kb-loader.
 *
 * The type import is a plain structural match against KbQuizQuestion so bun
 * typechecks the bank without forcing a circular import.
 */
import type { KbQuizQuestion } from "@/lib/kb-loader";

export const FINAL_EXAM_BANK: KbQuizQuestion[] = [];
